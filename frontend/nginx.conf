server {
    listen 8000;

    root /usr/share/nginx/html;
    index index.html;

    # React Router — fall back to index.html for all non-file routes
    location / {
        try_files $uri $uri/ /index.html;
    }
}
